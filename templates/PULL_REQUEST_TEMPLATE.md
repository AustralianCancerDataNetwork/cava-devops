## Summary

<!-- What does this PR do? This text is used as the changelog entry for this release — write it for a reader who will see it months from now, not for the reviewer reading the diff. -->

## Checklist

- [ ] Applied exactly one bump label (`major`, `minor`, `patch`, `fix`, `feature`, `breaking`, `chore`, or `skip-changelog`)
- [ ] Tests pass locally (`uv run pytest -q`)
- [ ] Lint passes (`uv run ruff check .`)
